import { Clinician } from "./types";

export const mapDays = (
  clinician: Clinician,
  trainingDays: number,
  vacationSchedule: string[],
  holidaySchedule: string[]
): string[] => {
  const days: string[] = [];
  return days;
};
